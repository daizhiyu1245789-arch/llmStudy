# 大模型应用实战练习

> 对应 `study/05_大模型应用实战/`。这一组练习的目标是：每一章都要跑起来、改一处、解释清楚。

---

## 01_把 tinyllm 跑起来

对应章节：

```text
05_大模型应用实战/01_把tinyllm跑起来.md
```

### 基础练习

- [ ] 安装依赖
- [ ] 跑一次 50 steps 训练
- [ ] 找到 `checkpoints/latest.pt`
- [ ] 用 `tinyllm.generate` 生成文本
- [ ] 解释 `loss` 日志

### 实战练习

分别运行：

```bash
python -m tinyllm.generate --ckpt_path checkpoints/latest.pt --prompt "今天" --temperature 0.3
python -m tinyllm.generate --ckpt_path checkpoints/latest.pt --prompt "今天" --temperature 1.2
```

记录：

- [ ] 哪个更稳定
- [ ] 哪个更随机
- [ ] 有没有乱码
- [ ] 你认为原因是什么

---

## 02_用自己的数据训练

对应章节：

```text
05_大模型应用实战/02_用自己的数据训练.md
```

### 基础练习

- [ ] 准备一个自己的 `.txt`
- [ ] 确认文本编码是 UTF-8
- [ ] 用自己的文本训练
- [ ] 对比 sample.txt 和自己数据的生成风格

### 实战练习

准备一个 `data/my_notes.txt`：

```text
写 20-50 行你自己的学习笔记。
```

训练：

```bash
python -m tinyllm.train --data_path data/my_notes.txt --max_steps 200
```

生成：

```bash
python -m tinyllm.generate --ckpt_path checkpoints/latest.pt --prompt "我学到"
```

记录：

- [ ] 模型有没有学到你的语气
- [ ] 是否过拟合
- [ ] 数据量够不够

---

## 03_部署成 API 服务

对应章节：

```text
05_大模型应用实战/03_部署成API服务.md
```

### 基础练习

- [ ] 解释为什么要把模型封装成 API
- [ ] 写出 `/generate` 的请求格式
- [ ] 写出 `/generate` 的响应格式
- [ ] 前端能调用接口

### 实战练习

设计接口：

```text
POST /generate
```

请求：

```json
{
  "prompt": "今天",
  "maxNewTokens": 80,
  "temperature": 0.8
}
```

响应：

```json
{
  "text": "..."
}
```

要求：

- [ ] 参数校验
- [ ] 错误处理
- [ ] 返回生成文本

---

## 04_Node 集成 Hugging Face

对应章节：

```text
05_大模型应用实战/04_Node集成HuggingFace.md
```

### 基础练习

- [ ] 进入 `examples/node-huggingface`
- [ ] 安装依赖
- [ ] 复制 `.env.example` 为 `.env`
- [ ] 配置 `HF_TOKEN`
- [ ] 启动服务
- [ ] 打开测试页面

### 实战练习

调用普通生成：

```bash
curl -X POST http://localhost:3001/api/generate \
  -H "Content-Type: application/json" \
  -d '{"prompt":"Deep learning is","maxNewTokens":80}'
```

调用流式生成：

```bash
curl -N -X POST http://localhost:3001/api/generate/stream \
  -H "Content-Type: application/json" \
  -d '{"prompt":"What is deep learning?","maxNewTokens":80}'
```

记录：

- [ ] 普通生成和流式生成的体验差异
- [ ] 如果 token 错了，会发生什么
- [ ] 如果 prompt 为空，会发生什么

---

## 05_Prompt 模板和结构化输出

对应章节：

```text
05_大模型应用实战/05_Prompt模板和结构化输出.md
```

### 基础练习

- [ ] 写一个编程老师 Prompt
- [ ] 加入 role/task/rules/output
- [ ] 加入 `PROMPT_VERSION`
- [ ] 让模型输出 JSON
- [ ] 用 Zod 校验 JSON

### 实战练习

实现：

```ts
buildExtractPrompt(text: string)
parseExtractResult(output: string)
```

输出格式：

```json
{
  "title": "string",
  "summary": "string",
  "keywords": ["string"],
  "todoItems": ["string"]
}
```

要求：

- [ ] 模型只输出 JSON
- [ ] 后端清理 Markdown fence
- [ ] Zod 校验
- [ ] 解析失败时返回明确错误

---

## 06_Embedding 和向量检索

对应章节：

```text
05_大模型应用实战/06_Embedding和向量检索.md
```

### 基础练习

- [ ] 解释 embedding
- [ ] 解释 cosine similarity
- [ ] 写 `chunkText`
- [ ] 设计 `DocumentChunk`
- [ ] 写内存 vector store

### 实战练习

用假 embedding 做检索：

```ts
const docs = [
  { content: "Node SSE 流式输出", embedding: [1, 0] },
  { content: "晚饭吃火锅", embedding: [0, 1] },
];
const query = [0.9, 0.1];
```

要求：

- [ ] 算相似度
- [ ] 排序
- [ ] 返回 top 1
- [ ] 解释为什么命中第一个

---

## 07_RAG 文档问答

对应章节：

```text
05_大模型应用实战/07_RAG文档问答.md
```

### 基础练习

- [ ] 写出 RAG 入库链路
- [ ] 写出 RAG 问答链路
- [ ] 写 `buildRagPrompt`
- [ ] 返回 `answer + sources`
- [ ] 解释检索错和生成错的区别

### 实战练习

用 3 个手写 chunk 模拟 RAG：

```text
chunk1: Node Hugging Face 示例启动方式
chunk2: RAG 是检索增强生成
chunk3: Tool Calling 让模型调用工具
```

要求：

- [ ] 用户问“怎么启动 Node 示例”
- [ ] 检索出 chunk1
- [ ] 拼进 prompt
- [ ] 回答里带 `[1]`
- [ ] sources 返回来源

---

## 08_Tool Calling 工具调用

对应章节：

```text
05_大模型应用实战/08_ToolCalling工具调用.md
```

### 基础练习

- [ ] 设计 `getWeather` 工具
- [ ] 写工具 schema
- [ ] 写工具 handler
- [ ] 用 Zod 校验参数
- [ ] 写 `toolRegistry`
- [ ] 限制最多调用 3 次

### 实战练习

模拟模型输出：

```json
{
  "type": "tool_call",
  "toolName": "getWeather",
  "arguments": {
    "city": "杭州"
  }
}
```

要求：

- [ ] 解析 JSON
- [ ] 校验 action
- [ ] 调用工具
- [ ] 把工具结果交回模型或拼最终回答

---

## 09_日志评估和部署

对应章节：

```text
05_大模型应用实战/09_日志评估和部署.md
```

### 基础练习

- [ ] 给请求加 requestId
- [ ] 记录 latencyMs
- [ ] 记录 model
- [ ] 记录 promptVersion
- [ ] 记录 RAG sources
- [ ] 设计限流策略
- [ ] 设计缓存策略

### 实战练习

写一个最小评估集：

```json
[
  {
    "id": "node-hf-start",
    "question": "Node Hugging Face 示例怎么启动？",
    "expectedKeywords": ["npm install", ".env", "npm run dev"]
  }
]
```

要求：

- [ ] 至少 5 条问题
- [ ] 每条有 expectedKeywords
- [ ] 手动跑一次
- [ ] 记录哪条失败

### 最终验收

你能说清：

```text
一个大模型应用上线后，要看效果、看成本、看错误、看延迟。
```
